import { KkDirective } from './kk.directive';

describe('KkDirective', () => {
  it('should create an instance', () => {
    const directive = new KkDirective();
    expect(directive).toBeTruthy();
  });
});
